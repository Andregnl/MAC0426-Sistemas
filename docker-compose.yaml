services:
  # web:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.web
  #   ports:
  #     - "3000:3000"
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
    network_mode: host
  